#!/usr/bin/env bash
# PATH shim so Cursor SCM finds our wrapper before /usr/local/bin/git.
# remoteEnv in .devcontainer/devcontainer.json prepends this directory.
exec "$(cd "$(dirname "$0")" && pwd)/../cursor-git.sh" "$@"
