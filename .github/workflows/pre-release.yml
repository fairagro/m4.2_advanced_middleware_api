name: Pre Release

permissions:
  contents: write
  packages: write
  security-events: write

on:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  code-quality:
    name: Code Quality
    # SHA pin: Devinfra tip that includes #222 (quality CLI --with-requirements / run-quality-cli.sh).
    uses: fairagro/m4.2_middleware_devinfra/.github/workflows/reusable-code-quality.yml@371ac9cefdf4c36ddc906d3ddff9e92e0b7af0cf
    with:
      python_package_root: middleware
      components: '["api"]'
      mypy_path: middleware/api/src:middleware/api_client/src:middleware/shared/src:middleware/api/tests/unit:middleware/api_client/tests/unit:middleware/shared/tests
      pylint_source_roots: middleware/api/src,middleware/api/tests/unit,middleware/api_client/tests/unit,middleware/shared/tests
    secrets: inherit

  build:
    name: Build
    uses: fairagro/m4.2_middleware_devinfra/.github/workflows/reusable-build.yml@main
    with:
      version_bump: 'patch'
      components: '["api"]'
      image_base_name: fairagro-advanced-middleware
    secrets: inherit

  check:
    name: Check
    needs: build
    # Local until Devinfra #74: omit Trivy licence-check (Alpine GPL noise).
    uses: ./.github/workflows/reusable-check-local.yml
    with:
      version: ${{ needs.build.outputs.version }}
      components: '["api"]'
      image_base_name: fairagro-advanced-middleware
    secrets: inherit

  release:
    name: Release
    needs: [build, check, code-quality]
    uses: fairagro/m4.2_middleware_devinfra/.github/workflows/reusable-release.yml@main
    with:
      version: ${{ needs.build.outputs.version }}
      pep440_version: ${{ needs.build.outputs.pep440_version }}
      components: '["api"]'
      image_base_name: fairagro-advanced-middleware
      release_type: 'feature'
      create_github_release: false
      tag_prefix: 'docker-v'
    secrets: inherit
