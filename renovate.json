{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",

  "extends": [
    "config:recommended"
  ],

  "timezone": "Europe/Berlin",

  "enabledManagers": [
    "dockerfile",
    "devcontainer",
    "pep621",
    "github-actions",
    "custom.regex"
  ],

  "rangeStrategy": "auto",

  "prHourlyLimit": 2,
  "prConcurrentLimit": 5,

  "labels": ["dependencies"],

  "rebaseWhen": "behind-base-branch",

  "commitMessagePrefix": "deps:",

  "lockFileMaintenance": {
    "enabled": true,
    "schedule": ["before 3am on monday"],
    "recreateWhen": "auto"
  },

  "devcontainer": {
    "fileMatch": [
      "(^|/)\\.devcontainer(/.+)?/devcontainer\\.json$"
    ]
  },

  "customManagers": [
    {
      "description": "Python patch pin in .python-version (SoT for runtime)",
      "customType": "regex",
      "fileMatch": ["^\\.python-version$"],
      "matchStrings": [
        "^(?<currentValue>\\d+\\.\\d+\\.\\d+)\\s*$"
      ],
      "datasourceTemplate": "docker",
      "depNameTemplate": "python",
      "extractVersionTemplate": "^(?<version>\\d+\\.\\d+\\.\\d+)$",
      "versioningTemplate": "loose"
    },
    {
      "description": "Alpine patch pin in .alpine-version (SoT; minor derived for python/apk)",
      "customType": "regex",
      "fileMatch": ["^\\.alpine-version$"],
      "matchStrings": [
        "^(?<currentValue>\\d+\\.\\d+\\.\\d+)\\s*$"
      ],
      "datasourceTemplate": "docker",
      "depNameTemplate": "alpine",
      "extractVersionTemplate": "^(?<version>\\d+\\.\\d+\\.\\d+)$",
      "versioningTemplate": "loose"
    }
  ],

  "packageRules": [
    {
      "description": "🔒 Python 3.12 global lock (.python-version + Docker alpine tags)",
      "matchManagers": ["dockerfile", "devcontainer", "github-actions", "custom.regex"],
      "matchPackagePatterns": ["python"],
      "allowedVersions": "/^3\\.12/",
      "groupName": "Python 3.12 runtime"
    },

    {
      "description": "🔷 Runtime Sync: Docker + Devcontainer base images",
      "matchManagers": ["dockerfile", "devcontainer"],
      "matchPackagePatterns": [
        "alpine",
        "mcr.microsoft.com/devcontainers"
      ],
      "groupName": "Runtime base (Docker + Devcontainer)"
    },

    {
      "description": "🧩 Devcontainer features",
      "matchManagers": ["devcontainer"],
      "matchPackagePatterns": ["features/*"],
      "groupName": "Devcontainer features",
      "separateMinorPatch": true
    },

    {
      "description": "🐍 Python application dependencies (uv / pep621)",
      "matchManagers": ["pep621"],
      "groupName": "Python dependencies (uv)",
      "rangeStrategy": "widen",
      "separateMinorPatch": true
    },

    {
      "description": "🐍 Python build tools (pip/uv/pyinstaller via regex)",
      "matchDatasources": ["pypi"],
      "groupName": "Python build tools"
    },

    {
      "description": "⚙️ GitHub Actions updates (stable grouping)",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["patch", "minor"],
      "groupName": "GitHub Actions"
    },

    {
      "description": "⚙️ GitHub Actions major updates",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["major"],
      "groupName": "GitHub Actions (major)"
    },

    {
      "description": "🐧 Alpine OS safe updates only",
      "matchDatasources": ["repology"],
      "matchUpdateTypes": ["patch"],
      "groupName": "Alpine OS safe updates"
    },

    {
      "description": "🚫 Disable risky OS upgrades",
      "matchDatasources": ["repology"],
      "matchUpdateTypes": ["minor", "major"],
      "enabled": false
    }
  ],

  "ignorePaths": [
    "**/node_modules/**",
    "**/__pycache__/**"
  ]
}
