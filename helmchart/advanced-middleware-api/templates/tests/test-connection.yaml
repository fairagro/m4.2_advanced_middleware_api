apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "advanced-middleware-api.fullname" . }}-test-connection"
  labels:
    {{- include "advanced-middleware-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "advanced-middleware-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
