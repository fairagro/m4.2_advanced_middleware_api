apiVersion: v1
kind: Service
metadata:
  name: {{ include "advanced-middleware-api.fullname" . }}
  labels:
    {{- include "advanced-middleware-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "advanced-middleware-api.selectorLabels" . | nindent 4 }}
